version: 0.2

env:
  secrets-manager:
    DATABASE_URL: RGB_RDS_DATABASE_URL

phases:
  install:
    runtime-versions:
      nodejs: 20
    commands:
      - npm install

  build:
    commands:
      - npx prisma migrate deploy
